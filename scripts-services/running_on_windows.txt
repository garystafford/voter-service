$env:ACTIVE_PROFILE = "docker-local"
$env:ENVIRONMENT = "development"

docker-compose `
  -f docker-compose-local.yml `
  -p demostack up `
  --force-recreate -d

# http://localhost:8090/haproxy?stats
# http://localhost:8090/voter/info
